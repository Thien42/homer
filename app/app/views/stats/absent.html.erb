<div class="container">
  <h1 class="text-center">Liste des absents au <%= @objective.objective_type %> de <%= @objective.project.name %></h1>

  <% @missing.each do |m| %>
      <div class="col-sm-10">
        <h3><%= m.email %></h3>
      </div>
      <div class="col-sm-2">
        <% if current_user.role == 1 %>
            <%= link_to "Supprimer ce supporter", projects_remove_user_path({:user_id => m.id, :project_id => @objective.project.id, :objective_id => @objective.id}), :class => "btn btn-danger", :method => :post, data: { confirm: "Êtes-vous sûr ? Cette action est irréversible"} %>
        <% end %>
      </div>

  <% end %>
</div>
