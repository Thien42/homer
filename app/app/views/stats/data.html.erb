<div class="container">
  <h2 class="text-center">Total des <%= money_name %> disponibles : <%= @spices %></h2>
  <table class="table">
    <thead>
    <tr>
      <th>email</th>
      <th><%= money_name %></th>
    </tr>
    </thead>
    <tbody>
    <% @users.each do |u| %>
        <tr>
          <td><%= u.email %></td>
          <td><%= u.spices %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>
